[project]
name = "smart-detector"
authors = ["Smart Detector Team"]
description = "A decentralized health metrics tracking platform"
telemetry = false
requirements = []
[contracts.health-metrics-tracker]
path = "contracts/health-metrics-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
